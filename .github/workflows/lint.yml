name: lint
on: [push]
env:
    YOUR_NAME: wang
jobs:
    eslint:
        runs-on: ubuntu-latest
        env:
            YOUR_SEX: boy
        steps:
            - name: test
              env:
                  YOUR_AGE: 18
              run: echo "Hello, ${{env.YOUR_NAME}}, your sex is ${{jobs.eslint.env.YOUR_SEX}}, your age is $YOUR_AGE"
