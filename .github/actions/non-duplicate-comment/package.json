{
  "name": "non-duplicate-comment",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir dist && cp src/* dist/ && echo \"build\""
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  }
}